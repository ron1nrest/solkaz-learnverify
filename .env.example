# Copy to .env — never commit .env

# Optional: Helius devnet (secondary RPC)
# HELIUS_API_KEY=

# Optional: RPC overrides
# LEARNVERIFY_RPC_PRIMARY=https://api.devnet.solana.com
# LEARNVERIFY_RPC_SECONDARY=

# Local E2E only — JSON array secret key bytes
# DEVNET_TEST_WALLET_SECRET=
